import 'package:flutter/material.dart';

class ClientsNewScreen extends StatelessWidget {
  const ClientsNewScreen(BuildContext context, {Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
